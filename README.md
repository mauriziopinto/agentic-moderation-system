# Agentic AI Content Moderation System

## Overview

This project implements a single-agent content moderation system using OpenAI's function calling API with a ReAct (Reason + Act) reasoning loop. The agent analyzes user-generated text against platform policies and recommends moderation actions.

## Agent Architecture

- **Type:** Single agent with ReAct reasoning loop
- **Model:** GPT-4o-mini via OpenAI API
- **Tools (5):**
  - *Text pattern analysis* — scans content for profanity, hate speech keywords, threats, and suspicious patterns like excessive capitalization or URLs
  - *Policy lookup* — retrieves the relevant platform policy for a specific violation category (e.g., hate speech rules and severity definitions)
  - *User history check* — looks up the content author's past moderation record to identify repeat offenders
  - *Severity assessment* — maps detected violations to a severity level (low, medium, high, critical) based on policy definitions
  - *Decision recording* — logs the final moderation action (approve, flag for review, or reject) with confidence score and explanation
- **Memory:** Conversation history (working memory), policy database, moderation history
- **Safeguards:** Confidence threshold (0.7), iteration limit (15), high-severity override, action constraints

## Policy Categories

The agent evaluates content across 6 categories:

1. Hate speech
2. Harassment
3. Violence
4. Spam
5. Misinformation
6. Adult content

## Setup

### Prerequisites

- Python 3.11+
- [uv](https://docs.astral.sh/uv/) package manager
- OpenAI API key

### Installation

```bash
cd 07_Design_of_Autonomous_and_Semi_Autonomous_Agentic_Workflows
uv venv
source .venv/bin/activate
uv sync
```

### API Key

Create a `.env` file in the project root:

```
OPENAI_API_KEY=your-api-key-here
```

### Run

Open and execute the notebook:

```bash
jupyter notebook agentic_system.ipynb
```

Or run all cells from the command line:

```bash
jupyter nbconvert --to notebook --execute agentic_system.ipynb
```

## Files

| File | Description |
|------|-------------|
| `agentic_system.ipynb` | Main notebook with agent implementation, test cases, and evaluation |
| `pyproject.toml` | Project dependencies |
| `uv.lock` | Reproducible dependency lock |
| `requirements.txt` | Generated for submission |
| `architecture_diagram.png` | Agent architecture diagram (generated by notebook) |
| `.env` | API key (not committed) |

## Test Scenarios

The notebook includes 5 test cases demonstrating:

1. Clear hate speech detection and rejection
2. Borderline political debate flagged for human review
3. Legitimate news content approved
4. Medical/educational context correctly identified
5. Coded language failure case (known limitation)
